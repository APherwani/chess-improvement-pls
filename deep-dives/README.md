# deep-dives

Use sparingly.

Deep dives are allowed only when multiple games show that a theme deserves extra study.

Good examples:

- repeated failure in one pawn structure
- recurring exchange evaluation error
- repeated failure to convert the same endgame type

